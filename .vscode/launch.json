{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug executable 'libchai'",
      "cargo": {
        "args": ["build", "--bin=chai", "--package=libchai"],
        "filter": {
          "name": "chai",
          "kind": "bin"
        }
      },
      "args": ["optimize"],
      "cwd": "${workspaceFolder}"
    }
  ]
}
